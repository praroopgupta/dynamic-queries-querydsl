server:
  port: 8080
logging:
  level:
      org.springframework: INFO
spring:
  datasource:
      url: jdbc:mysql://${Local instance MySQL80:localhost}:3306/dev
      username: root
      password: password
  jpa:
      hibernate.ddl-auto: update
      show-sql: true
  security:
    user:
        name: username
        password: password